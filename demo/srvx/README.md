# LiquidJS with srvx

ESM server using [srvx](https://srvx.h3.dev). Templates are rendered with `renderFile()` and returned as a web `Response`.

```bash
cd demo/srvx
npm install
npm start
```

Open http://127.0.0.1:3000
